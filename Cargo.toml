[package]
name = "millify-rs"
version = "0.1.0"
edition = "2024"

[lib]
name="millify"
crate-type = ["lib"]

[dependencies]
icu = "2.2.0"
rust_decimal =  { version = "1.42.0", optional = true }
num-traits = "0.2.19"

[features]
default= []
rust_decimal = ["dep:rust_decimal"]
